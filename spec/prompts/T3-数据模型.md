# T3: 数据模型定义

## 任务目标
定义所有数据模型类，支持序列化和不可变更新。

## 设计参考
- `/Users/victor/Documents/codes/flutter/receipt-printing/spec/菜单功能 spec.md` - 菜品模型
- `/Users/victor/Documents/codes/flutter/receipt-printing/spec/点单功能 spec.md` - 订单模型
- `/Users/victor/Documents/codes/flutter/receipt-printing/spec/日总结功能 spec.md` - 日总结模型
- `/Users/victor/Documents/codes/flutter/receipt-printing/spec/小票打印功能 spec.md` - 打印机配置

## 模型定义

### Dish (菜品)
```dart
class Dish {
  final int? id;
  final String name;           // 必填
  final double? price;         // 可选
  final String? imagePath;     // 可选
  final int sortOrder;         // 排序，默认0
  final DateTime createdAt;
  final DateTime updatedAt;

  // 方法: fromJson, toJson, copyWith, ==, hashCode, toString
}
```

### Order (订单)
```dart
class Order {
  final int? id;
  final int ticketNumber;      // 取餐号
  final int dishId;            // 菜品ID
  final String dishName;       // 菜品名称（冗余存储，防止菜品被删除后无法显示）
  final DateTime createdAt;

  // 辅助方法: get date => YYYY-MM-DD

  // 方法: fromJson, toJson, copyWith, ==, hashCode
}
```

### DailySummary (日总结)
```dart
class DailySummary {
  final DateTime date;
  final int totalOrders;
  final List<DishSummary> dishSummaries;
  final Map<int, int> hourlyDistribution;  // 小时(0-23): 订单数

  // 方法: 构造函数, ==, hashCode
}

class DishSummary {
  final int dishId;
  final String dishName;
  final int count;

  // 方法: 构造函数
}
```

### PrinterConfig (打印机配置)
```dart
class PrinterConfig {
  final String? deviceAddress;    // 蓝牙设备MAC地址
  final String? deviceName;       // 设备名称（用于显示）
  final bool printShopName;       // 是否打印店名
  final String? shopName;         // 店名内容
  final bool printDateTime;       // 是否打印日期时间
  final bool printTwoCopies;      // 是否打印两联
  final bool dualPrinterMode;     // 是否双打印机模式（预留）
  final String? customerPrinterAddress;  // 顾客联打印机（预留）
  final String? kitchenPrinterAddress;   // 厨房联打印机（预留）

  // 方法: fromJson, toJson, copyWith, 默认值
}
```

## 技术要求
- 使用 `@immutable` 注解
- 所有字段都是 final
- copyWith 支持 null 值更新（使用 Null 包装类或明确参数）
- fromJson 要能处理数据库返回的 Map（字段名匹配）

## 输出文件
- `lib/models/dish.dart`
- `lib/models/order.dart`
- `lib/models/daily_summary.dart`
- `lib/models/printer_config.dart`

## 验证标准
- [ ] 所有模型能正常序列化/反序列化
- [ ] copyWith 工作正常
- [ ] 相等性判断正确
